apiVersion: apps/v1
kind: Deployment
metadata:
  name: sk-multiagent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sk-multiagent
  template:
    metadata:
      labels:
        app: sk-multiagent
    spec:
      containers:
      - name: sk-app
        image: gcr.io/polar-scarab-145015/sk-agent:latest 
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"        
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: GEMINI_API_KEY         
          valueFrom:
            secretKeyRef:
              name: gemini-secret
              key: ***